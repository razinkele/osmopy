---
name: project-backlog-infra-viewers
description: "4-stage backlog plan + Map-View fix shipped 2026-06-13 across PRs #54-#59 (master 81d6609): py3.13 CI matrix, narrative docs, per-cell NetCDF viewer, Pareto picker, Map-View species rendering"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3c92cf51-9a04-490e-b26d-2e39302eb27f
---

**Backlog plan COMPLETE + shipped (master `81d6609`):** (1) CI matrix ✅ PR#54, (2) narrative docs ✅ PR#56, (3) per-cell NetCDF viewer ✅ PR#57, (4) Pareto picker ✅ PR#58, + Map-View species-rendering fix ✅ PR#59. Plan doc `docs/plans/2026-06-13-backlog-infra-and-viewers-plan.md`.

**Map-View species-rendering fix SHIPPED 2026-06-13** (PR#59, master `81d6609`). Spatial Results Map/Flat views rendered NOTHING for engine output: engine writes `(time, species, lat, lon)`, renderers did `isel(time)` but not species → 3-D slice → `ndim!=2` early-return ("Expected 2D spatial slice"). Fix: `osmose/spatial_series.spatial_slice_2d` collapses species (sum/select) → 2-D; wired into `make_spatial_map` + the deck.gl map effect; new "Species" selector on the page. **2nd bug found by live render: Flat heatmap land-NaNs broke shinywidgets `json(allow_nan=False)` → map NaN→None (gaps).** 4 new tests. Side effect: deck.gl **click-to-select** (wired dead in Stage 3) is now live since the map renders cells. Validation substrate (no shipped config emits spatial output): synthetic spatial NetCDF + real minimal CSVs in a repo-internal dir, loaded via Results' "Load Results" (needs biomass CSV + dir inside cwd).

**Stage 4 (Pareto picker) SHIPPED 2026-06-13** (PR#58, master `0ade369`, branch deleted). New `osmose/calibration/pareto.py` (`nondominated_indices`/`select_solution`/`solution_overrides_csv`, 8 tests) + picker UI on calibration Results→Best Parameters (`input_select` of non-dominated solutions + selected-solution panel + CSV export of `key ; value` overrides). **Fixed a pre-existing DEAD PATH the recon found:** History→Load→Pareto expected `parameters` as list[dict] + `results.pareto_X/F`, but the live NSGA-II writer (`_save_run_for_nsga2`) saved `parameters` as list[str] and NO front → loading any real run crashed. Fix: writer now persists `pareto_X/pareto_F/objective_names`; load handler accepts both param shapes + single-objective (no-front) runs without crashing; added shared `cal_param_keys` reactive (full keys) for export. Playwright-validated (NSGA-II→pick→export exact; DE→empty-state no-crash). Gotcha: `_non_dominated_indices` lives in `surrogate.py:12` (private); pareto.py re-exports it publicly to avoid the UI reaching into a private symbol.

**Stage 3 SHIPPED 2026-06-13** (PR#57 rebase-merged, master `14f0093`, branch deleted). `osmose/spatial_series.py` (`cell_timeseries` path + `cell_timeseries_from_dataset` open-ds variant, lazy isel-then-.values, species sum/mean/select, land→NaN; 12 tests) + "Cell Series" nav_panel in `spatial_results.py` (row/col pickers + deck.gl `spatial_map_click` sync + species selector → plotly line + empty states). **Live Playwright caught a real bug the 12 unit tests missed: re-opening the page's already-open NetCDF → "NetCDF: HDF error" → fixed via the open-ds variant.** Backend defines dim-name sets locally (no `ui/` import — core stays UI-independent). Gotcha: existing Map View can't render the engine's 4-D species-dimmed spatial var ("Expected 2D spatial slice") → used numeric pickers as primary, click syncs them; minimal config's spatial output is all-NaN (no ocean) so dev/Playwright substrate = synthetic NetCDF + real CSVs in a repo-internal dir.

**Stage 1 SHIPPED 2026-06-13** (PR #54 rebase-merged, master `032a87e`, branch deleted). `type-check` job matrixed 3.12+3.13 via `pyright --pythonversion ${{ matrix.python-version }}` + `fail-fast: false`. **Round-3 review caught the BLOCKER:** `pyrightconfig.json` pins `pythonVersion: "3.12"` (overrides interpreter), so a bare matrix is a NO-OP — must pass `--pythonversion` per leg. lint/docker stay single-version by design. CI all 6 legs green. **Node20 action-deprecation resolved via PR #55 (master `4d22db9`):** checkout@v5/setup-python@v6/upload-artifact@v6 (each first Node24 major; upload-artifact v5 is still node20).
