---
name: project-config-key-migration-440
description: OSMOSE 4.4.0 config-key migration — PR1 (core) + PR2 (UI/writer/calibration wiring) SHIPPED 2026-06-18; jar-swap is the only remaining step
metadata: 
  node_type: memory
  type: project
  originSessionId: 3c92cf51-9a04-490e-b26d-2e39302eb27f
---

Migrating OSMOPY to OSMOSE Java **4.4.0** config keys, so OSMOPY can eventually bundle the 4.4.0 engine ([[reference-osmose-java-4-4-0]]). 4.4.0 refuses 4.3.x configs and its own `-update` crashes on ours, so OSMOPY ports OSMOSE's `Releases.java $15` migration itself. Sequenced PR1 (core) → PR2 (UI/writer wiring) → jar swap.

## UPDATE 2026-06-26 — Java 4.4.1 EEC adoption (Phase 0+1+2) SHIPPED to local master (merge `a421681`; note-fix `77668fb`). NO CUTOVER (default engine + write target stay 4.3.3; inert).
- **Phase 1 resource-forcing migration DONE:** `_emit_resource_biomass_forcing` in `aliases.py`'s `to_target_keys` `>=4.4.0` branch emits the `species.biomass.{file,varname,nsteps.year,mode}.spN` keys 4.4.x `ResourceForcing.init()` hard-requires (varname = `species.name.spN`, 1:1 with the EEC NetCDF vars). **EEC now LOADS + runs on the 4.4.1 jar.** 4.4.1 also needs the config stamped `osmose.version=4.4.1` (write `target_version="4.4.1"`) to clear its version gate; the jar's own `-update` does NOT synthesize the resource-forcing keys (so it can't self-migrate). 4.4.1 jar sha256 `8ae7c519fc5456beb6419306876b74b8ce074020fb406fa8c198fa18cf697cee`.
- **Phase 2 cross-engine parity PASS** (`scripts/cross_engine_parity_440.py`, N=16, 4 metrics biomass/yield/abundance/mean-weight, formal TOST+MEF+KS+relative-gate): Java-4.4.1 agrees with the Python engine **indistinguishably from 4.3.3** (MEF 0.95–0.98, relative gate Y for all 14 EEC species, zero collapses). The earlier single-seed sardine/mackerel "collapse" scares were stochastic.
- **▶▶ DOGFISH/MACKEREL ~3× GAP — DEEP-INVESTIGATED (2026-06-26, systematic-debugging). It is TWO different causes, both within the accepted ±1-OoM parity bar; the swap gate is relative + passed, so neither blocks 4.4.1.**
  - **SIZE RATIOS — REFUTED (do NOT re-chase).** Inverted-ratio handling is IDENTICAL in both engines: Java (`PredationMortality.getAccessibility/init`: `preyMin=L/min, preyMax=L/max`, swaps when max>min) and Python (`config.py:664` + `predation.py:451-452`: `preyMin=L/max, preyMax=L/min`, swaps when min>max) are exact mirror images → both yield prey window `[L/max(a,b), L/min(a,b)]`.
  - **MACKEREL — ROOT CAUSE CONFIRMED: larval additional-mortality formulation differs.** Java `AnnualLarvaMortality.getRate` applies `rate/nStepYear` per-timestep over the larval stage (×seasonality, ×density-dep); Python `natural.py:103` applies the **full rate once** to eggs (its docstring's "matches Java" claim is WRONG). Decisive test: zeroing `mortality.additional.larva.rate.sp10` collapsed the gap 21.5×→1.4×. Mackerel is uniquely exposed (sole additional mortality is larval, adult M=0, rate 3.97 in the exp-sensitive band; cod rate 10.97 saturates → insensitive, agrees). **A fix is PARITY-CRITICAL (touches every species' recruitment) → own task w/ full EEC/BoB re-validation + baseline regen; within tolerance so not urgent.**
  - **DOGFISH — CONFIRMED a RECRUITMENT difference (recruit ratio 79× vs total 6.7×, instrumented).** Stable ~3–5× equilibrium (NOT spin-up). RULED OUT: size ratios, larval additional mortality (larva-OFF persists), spin-up, egg-size/weight inconsistency. Egg-production FORMULA matches Java (`sex_ratio×fecundity×SSB×season×1e6`); maturity normal. So the 79× over-production of dogfish recruits is in the egg→recruit pathway EXCLUDING larval additional mortality — egg production inputs (SSB/seeding) or the egg→recruit conversion for this extreme life history (relativefecundity 0.14, egg.size 9.5). Still not pinned to a line.
  - **READER FIX SHIPPED (master `7686cd9`):** `results.py` per-species distribution readers (`abundance_by_size` etc.) now reshape Java's CROSS-SPECIES `{metric}DistribBy{X}` files (`Time,<bin>,<species cols>` — NOT the per-species `{metric}By{X}` the in-memory engine uses). The "writer" was never broken; Java emits the files with `output.abundance.bysize.enabled=true`. Unblocks cross-engine size/age-structure comparison.
  - **MACKEREL FIX — root cause FULLY traced (source-verified), fix NOT done (parity-critical, deferred).** Exhaustively verified that EVERY structural piece MATCHES between engines for EEC: `is_egg = ageDt < firstFeedingAgeDt` (both); `firstFeedingAgeDt = 1` (both — EEC has no `species.first.feeding.age.spN`, so Java's default branch = 1 = Python's hardcoded `state.py:145 np.ones`); larval-rate formula `multiplier(1)×base×season(1)` (Java `AnnualLarvaMortality.init` bytecode); `SeasonTimeSeries` default = **1.0 per step** (the `1/nStepYear` line is commented out, master `SeasonTimeSeries.java:39`). DESPITE the match, larva-OFF empirically reduces Java mackerel ~47× but Python only ~3×. **The residual = larval-mortality APPLICATION SEQUENCING:** Python `natural.py` applies it SEPARATELY *before* the main mortality loop (docstring says so) → ~98% of mackerel eggs removed first, predation acts on survivors; Java applies it as ONE cause INSIDE the combined simultaneous mortality (predation+additional+starvation together). At mackerel's near-saturating rate (3.97→98%) sequential-vs-simultaneous diverge sharply. dogfish is the same family but broader (`first_feeding`-gated starvation/fishing/energy too → larva-OFF didn't fully close it). **Fix = restructure Python larval mortality into the combined mortality process — touches EVERY species' mortality resolution → needs baseline regeneration + full EEC/BoB re-validation. Within ±1-OoM tolerance. DO AS A DEDICATED TASK, do NOT rush (regression risk).**
- **Still deferred:** BoB (Java-blocked, 365-step daily NetCDF doesn't divide ndt=24); Phase 3 (ICES, plan calls it weak/EEC-uncalibrated); Phases 4–5 (the actual cutover + deploy). Resume the cutover only for a concrete 4.4.x feature need. Plan: `docs/superpowers/plans/2026-06-19-jar-swap-440-validated-resume.md`. Phase-2 results note: `docs/superpowers/notes/2026-06-26-jar-swap-440-phase2-eec-results.md`.

**PR1 — SHIPPED 2026-06-17/18, master `001c530` (PR #77).** Pure `osmose/` engine+machinery; canonical internal keys = 4.4.0, old read via the ported chain.
- `osmose/config/aliases.py`: `RENAMES_440` (16 renames, source-verified vs live `Releases.java $15` — incl. 2 the release notes OMITTED: `output.fecundity.bysize.enabled→output.number.of.eggs.bysize.enabled`, `population.initialization.relativebiomass.enabled→module.population.initialisation.enabled`); `canonicalize_config` (forward, idempotent, returns deprecated list); `to_target_keys` (inverse via `_INVERSE_440`, longest-prefix-first, **leaf-scoped maturity** so pre-existing `species.maturity.size/age` growth keys are untouched; ingestion merge non-invertible). Drift-guard test keeps `_INVERSE_440` faithful to `RENAMES_440`.
- `osmose/demo.py`: `("4.4.0", RENAMES_440)` chain entry; applier now uses **`updateKey` skip-if-target-exists** (Java "already defined" — **base/legacy value wins, NOT bioen**; 3 review rounds corrected this from a wrong "bioen-wins" guess). `_version_tuple("")→(0,)` hardened.
- `osmose/engine/config.py`: `from_dict` canonicalizes then reads NEW key names incl. the **unified** `predation.ingestion.rate.max.sp{i}` (was a separate `.bioen` read).
- Validators (`config_validation.validate`, `config/validator.validate_config`) canonicalize at entry; new + (PR1-bridge) old keys allowlisted.

**INTENDED behavior change (PR1):** 4.4.0 unifies the bioen + base max-ingestion-rate into ONE key; for a bioen config with both (e.g. `baltic_ev` cod), base wins → the bioen energy budget now uses the unified value. Non-bioen results byte-identical; parity baselines unaffected. Gated by `test_bioen_ingestion_unification_is_consistent`; only assertion changed = `test_bioen_predation_params_parsed` 4.2→3.5. **Changelog note for Ev-OSMOSE users is owed in PR2.**

**PR2 — SHIPPED 2026-06-18, master `dc2d701` (PR #78, squash; 7 tasks, plan `docs/superpowers/plans/2026-06-18-config-migration-440-pr2-wiring.md`).** Internal model now fully 4.4.0; engine-bound writes reverse-map to 4.3.x (bundled jar unchanged → no behavior change).
- Schema `key_pattern`s moved to NEW names + `_OUTPUT_ENABLE_FLAGS` (`output.fisheries.*`); PR1-bridge allowlist dropped. The bioen `predation.ingestion.rate.max.bioen` field was REMOVED (not renamed) — it collided with the pre-existing unified `predation.ingestion.rate.max.sp{idx}` in `schema/species.py` (the intended unification).
- Reader `read_file` canonicalizes per-file + KEEPS renamed-OLD `key_case_map` entries (so the writer's NEW→OLD reverse recovers camelCase source casing); exposes `reader.deprecated_keys`. Guards `migrate_config`'s unconditional `osmose.version` stamp on version-less sub-files.
- `AppState.load_config(cfg, case_map=None)` canonicalizes every load site (demo/scenario/fishbase); demo-load deprecation notice fires off `reader.deprecated_keys` (NOT load_config's return — the reader already canonicalized, so that return is always `[]`). `ScenarioManager.load` canonicalizes; save intentionally persists 4.4.0 keys.
- `OsmoseConfigWriter.write` + `write_temp_config` gain `target_version="4.3.3"` → reverse-map via `to_target_keys` before serialize/route (ROUTING table UNTOUCHED — deferred to jar-swap). `to_target_keys` hardened to collapse mixed old+new dicts (always-pop NEW, keep existing OLD). Export config inherits the 4.3.3 default.
- Calibration `-P` Java override keys reverse-mapped (skip the `osmose.version` stamp); checkpoint `param_keys`/`best_parameters`/`bounds_log10` forward-migrated on `read_checkpoint` via a DIRECT `RENAMES_440` prefix-map helper (NOT `canonicalize_config` — avoids the version-stamp leak into param_keys).
- Also fixed a missed call-site: `scripts/run_fie_demo.py` bioen guard read OLD `simulation.bioen.enabled` off a now-canonicalized config.

**REMAINING — the jar swap (only step left):** bundle `osmose_4.4.0-jar-with-dependencies.jar`, flip the write-default `target_version` to `4.4.0`, ADD `module.*`/`output.fisheries.*` to `OsmoseConfigWriter.ROUTING`, update hardcoded `4.3.3` refs (`ui/state.py:42`, `tests/test_state.py:79`, `demo.py` default), fold the cosmetic stale docstring `osmose/engine/processes/foraging_mortality.py:11`, refresh Java cross-engine parity. NOT a blind swap — needs the ROUTING additions + default flip together. Plans: PR1 `docs/superpowers/plans/2026-06-18-config-migration-440-pr1-core.md`, PR2 `...-pr2-wiring.md`.

Process note: spec 3 in-loop review rounds (scite confirmed eta/r/m0/m1=bioenergetic MRN vs l50/l75=logistic ogive; authoritative-source check fixed merge direction + found 2 renames) + 3 plan-review rounds (round 3 + a narrow round caught the checkpoint `osmose.version`-shim leak before execution). Built subagent-driven TDD (implementer→spec review→quality review per task); the two-stage reviews caught real cross-task bugs a single pass missed: dead deprecation notification, a write→read parity regression (version-gated `migrate_config`), the missed `run_fie_demo` call-site, and property-test/strategy breaks from read-time canonicalization. Final gate 3403 passed / 0 failed. **Prod redeploy (`sudo bash deploy.sh`) still owed — jar stays 4.3.3 so it's UI/writer wiring only.**

## Deep-review of the deferral / A-B gate failure (2026-06-21, multi-agent workflow, source-verified)
The PR-B cutover stayed deferred but the review **retargeted it to v4.4.1** and corrected the premise. **The A/B load failure was largely a GENUINE v4.4.0 engine bug:** v4.4.0 `ResourceForcing.java` gates the NetCDF branch on legacy `species.file.spN` but reads `species.biomass.file.spN` (gate/read key mismatch — v4.4.0 can't load its OWN bundled EEC NetCDF config). **v4.4.1 (released 2026-06-18, day before the deferral) fixes it** (gates on `species.biomass.file.spN`). Resume against v4.4.1: `gh release download v4.4.1 -R osmose-model/osmose -p '*jar-with-dependencies*' -D osmose-java/` (sha256 not on GitHub → capture at download).
- **Still required on v4.4.1:** `species.biomass.varname.spN` is hard-required (getString→fatal, no default); varname = `species.name.spN` (verified 1:1 with the 10 `eec_ltlbiomassTons.nc` data vars — NOT NetCDF-sniffed). The Phase-1 emit set `{mode,file,varname,nsteps.year}` is exactly the required NETCDF init surface, nothing forgotten. **Diagnosis + Phase-1 recipe = CORRECT.**
- **BoB (`data/examples`) is LESS work than the plan stated:** it already carries native `species.type=resource`/`species.name.sp8-13`/`nresource=6` on disk (1:1 with its NetCDF vars) → NO `ltl.*→species.*` identity normalization needed; the legacy `ltl.name/tl/size.rscN` keys are inert. The one real extra step over EEC = synthesize `species.file.spN`+`species.biomass.file.spN` from the global `ltl.netcdf.file`. **BoB real blocker:** its NetCDF has 365 daily steps which doesn't divide ndt=24 → likely incompatible with 4.4.x `NETCDF_BIOMASS` as-is (needs re-sampling).
- **Audit-completeness:** the migration mechanism is rename-only (`Releases.java` v4.4.0 entry is `updateKey`-only) → structurally blind to newly-required keys; the empirical fix-forward load loop is the right convergent method (already in the plan). Fatality rule: only `getString/getInt/getFloat/getDouble/getFile` are fatal on a missing key; `getBoolean` is canFind-guarded → non-fatal (don't migrate new booleans as required).
- **Validation fix:** Task 2.4 (Baltic config-round-trip) should be **bit-exact** (`simulation.rng.fixed=true` + fixed seed, tolerance 0), not statistical — it's a same-engine same-RNG round-trip; any diff = a migration defect. Phase 2's full TOST/MEF/power-N stack + Phase 3's eec/BoB ICES snapshot (Task 3.1) are arguably over-engineered (trim at resume).
- **Deferral STILL holds.** Note: "near-vestigial" is loose — Java is the UI DEFAULT engine (`ui/state.py:64`); cheap orthogonal fix = flip default to Python. Smallest de-risk at resume: Phase 0+1 ONLY against v4.4.1 (verify load), no cutover. Plan retargeted: master `d1aee23`.
